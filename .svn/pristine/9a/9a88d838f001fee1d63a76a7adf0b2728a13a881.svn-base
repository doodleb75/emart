// User Info Offcanvas Style
// -----------------------------------------------------------------------------

.offcanvas-bottom.user-info-offcanvas {
    height: auto; // 높이 자동 조절 (Auto height)
    min-height: rem(300); // 최소 높이 설정 (Min height)
    border-top-left-radius: rem(24); // 상단 좌측 라운드 (Top left radius)
    border-top-right-radius: rem(24); // 상단 우측 라운드 (Top right radius)
    border-top: none;
    padding-bottom: rem(16);

    .offcanvas-body {
        padding: 0 rem(16) rem(64);
    }

    // 상단 핸들 (Top Handle)
    .offcanvas-handle {
        width: rem(80);
        height: rem(4);
        background-color: $gray-200;
        border-radius: rem(5);
        margin: rem(10) auto rem(16);
        flex-shrink: 0;
        cursor: pointer;
        position: relative; // For pseudo-element
        touch-action: none; // 브라우저 기본 제스처 차단 (Prevent browser gestures)

        // 터치 영역 확장 (Expand touch area)
        &::after {
            content: '';
            position: absolute;
            top: -15px;
            bottom: -15px;
            left: -20px;
            right: -20px;
        }
    }

    .sep-divider {
        border-color: $gray-100;
        opacity: 1;
    }

    // 유저 상단 요약 (User Summary Header)
    .user-summary {
        display: flex;
        align-items: center;
        height: rem(32);
        gap: rem(8);
        margin-top: rem(16);
        margin-bottom: rem(16);
        // padding: rem(16) 0;

        .badge-gold {
            background-color: #FFAA00; // GOLD 배경색 (Gold bg)
            color: $white;
            height: 100%;
            line-height: 1.75;
            padding: rem(4) rem(8);
            border-radius: rem(4);
            font-size: rem(14);
            font-weight: 600;
            margin-right: rem(10);
        }

        .user-detail-text {
            font-size: rem(14);
            color: $gray-700; // #222 -> $gray-700 적용

            span {
                margin: 0 rem(4);
                color: $gray-200;
            }
        }
    }

    // 포인트 프로그레스 자리 (Point Progress Section)
    .point-progress-section {
        padding: rem(20) 0 rem(10);

        .point-progress-track {
            width: 100%;
            height: rem(40);
            background-color: $gray-50;
            border-radius: rem(100);
            position: relative;
            overflow: hidden;

            // 배경 패턴 (Background Pattern)
            // 10px 원 (rem(5) radius), 4px 간격 (gap)
            // 총 반복 너비: 10px + 4px = 14px
            background-image: radial-gradient(circle, $white rem(3), transparent rem(3));
            background-size: rem(20) 100%;
            background-position: center;
            background-repeat: repeat-x;
        }

        .point-progress-fill {
            height: 100%;
            background-color: $primary-gold;
            border-radius: rem(100);
            display: flex;
            align-items: center;
            justify-content: center;
            padding-left: rem(6);
            min-width: rem(40); // 아이콘이 잘리지 않도록 최소 너비 설정
            box-sizing: border-box;
            transition: width 0.5s ease-out;

            .icon-p {
                width: rem(28);
                height: rem(28);
                color: $white;
                font-weight: 700;
                font-size: rem(15);
                display: flex;
                align-items: center;
                justify-content: center;
                // P 자체가 원형 배경을 가질 수도 있으나, 여기서는 Fill이 배경 역할을 함.
                // 명시적으로 P만 강조하고 싶다면 background-color 추가 가능하지만,
                // "P area expands" 문맥상 Fill 전체가 Gold임.
            }
        }
    }

    .progress-info {
        display: flex;
        align-items: center;
        gap: rem(4);
        font-size: rem(14);
        color: $gray-300;
        padding: rem(6) 0;

        svg {
            color: $gray-300;
        }
    }

    // 유저 통계 리스트 (User Stat List)
    .user-stat-list {
        font-size: rem(14);
        line-height: 150%;
        padding: rem(0) 0;

        .stat-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: rem(12);
            padding: rem(6) 0;

            &:last-child {
                margin-bottom: 0;
            }

            .label {
                font-size: rem(14);
                color: $gray-700; // #222 -> $gray-700 적용
                display: flex;
                align-items: center;
                gap: rem(4);

                svg {
                    stroke: $gray-300;
                }
            }

            .value {
                font-size: rem(16);
                font-weight: 700;
                color: $gray-700; // #222 -> $gray-700 적용
                font-family: $font-family-number;
            }
        }
    }
}